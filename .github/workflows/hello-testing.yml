name: Hello on testing branch

on:
  push:
    branches:
      - "feat/test-auto-pr-github-actions"

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello world!"
